需满足的条件：
1，函数的作用域相同，且名称相同。
2，函数参数的类型、个数、顺序不同。
    —>返回值不同不能作为函数重载的条件
    ->当引用作为函数重载的条件时，调用相同的函数名，系统更倾向于调用变量而不是const修饰的只读不能写函数
        ->当给的实参是一个常量时，会调用const修饰的函数
3，函数重载遇到默认参数，即func(int a,int b = 10) 和 func(int a)，会出现二义性。